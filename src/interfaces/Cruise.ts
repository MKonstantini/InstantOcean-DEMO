export default interface Cruise {
    cruiseNum: number,
    name: string,
    duration: number,
    departFrom: string,
    ports: string,
    img: string,
    startDate: string,
    startPrice: number
}